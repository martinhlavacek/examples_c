#include <stdio.h>

int main() {
    int a = 1;
    int b = 0;

    if (a != 0 && b != 0) {
        printf("oba jsou pravda\n");
    } else {
        printf("na druhy operand se uz operace nedostane\n");
    }

    return 0;
}
